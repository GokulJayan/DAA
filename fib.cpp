#include<iostream>
using namespace std;

int fib(int n)
{
    int f[n+]
    if(n==1 || n==0)
    return n;
    else
    A[n]=fib(A[n-1])+fib(A[n-2]);
}
int main()
{
    int n;
    cout<<"Enter n: ";
    cin>>n;
    cout<<fib(n);
    return 0;
}